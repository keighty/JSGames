$(function() {
  var form = $('form');
  form.hide();

  $('.btn-primary').bind('click', function() {
    form.show();
    // create the secret code
    // add enter guess and text field to template
  });
});